# Recipe System

An offline recipe management and intelligent search system.

## Purpose

Store recipes in a structured, searchable format from raw sources such as images, PDFs, and text.

The system will use an offline AI model to extract and organize recipe information, allow the user to review and correct the extracted data, and then store the approved recipe in the local database.
